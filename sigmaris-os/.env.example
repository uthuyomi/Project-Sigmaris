# Frontend (Next.js / sigmaris-os)

# Supabase
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Server-side Supabase (Route Handlers insert messages/snapshots)
SUPABASE_SERVICE_ROLE_KEY=

# Backend Persona OS URL (FastAPI)
# Used by /api/aei to proxy to /persona/chat
SIGMARIS_CORE_URL=http://127.0.0.1:8000

# Optional: OpenAI for /api/summary and /api/translate in the frontend app
OPENAI_API_KEY=

# Ops: Code size (LOC) audit page (/audit/code-size)
# - Disabled by default in production. Enable explicitly if you want it exposed.
SIGMARIS_CODE_STATS_ENABLED=0
# Cache duration (ms) for repeated page refreshes
SIGMARIS_CODE_STATS_CACHE_TTL_MS=15000
