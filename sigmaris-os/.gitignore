# === Base: standard Next.js / TypeScript ignores ===

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# === Env files ===
# allow other env files (shared, example, dev, etc.)
!.env
!.env.*
!*.env
!*.env.*

# ignore ONLY local secrets (keep this AFTER the allow rules)
.env.local

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts
